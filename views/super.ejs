<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow">
  <title>Super Admin — Valle Vista Commons</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root { --vvc-green: #2d6a4f; --vvc-bg: #f0f4f0; }
    body { background: var(--vvc-bg); font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; }
    .brand { font-weight: 800; color: var(--vvc-green); text-decoration: none; }
    .section-title { font-weight: 700; font-size: 1rem; color: #333; border-bottom: 2px solid #6f42c1; padding-bottom: 4px; }
    .panel-card { background: #fff; border-radius: 10px; padding: 16px 20px; margin-bottom: 16px; box-shadow: 0 1px 2px rgba(0,0,0,0.05); }
    .btn-xs { font-size: 0.72rem; padding: 2px 10px; border-radius: 6px; }
    .mod-row { display: flex; justify-content: space-between; align-items: center; padding: 8px 0; border-bottom: 1px solid #eee; }
    .mod-row:last-child { border-bottom: none; }
    .status-active { color: #198754; font-weight: 600; font-size: 0.75rem; }
    .status-disabled { color: #dc3545; font-weight: 600; font-size: 0.75rem; }
    .model-note { font-size: 0.7rem; color: #888; }
  </style>
</head>
<body>
  <div class="container py-4" style="max-width: 700px;">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <a href="/" class="brand fs-5">Valle Vista Commons</a>
        <span class="text-muted" style="font-size: 0.8rem;"> / Super Admin</span>
      </div>
      <div class="d-flex gap-2">
        <a href="/admin" class="btn btn-outline-secondary btn-sm">Mod Panel</a>
        <a href="/" class="btn btn-outline-secondary btn-sm">View Board</a>
      </div>
    </div>

    <!-- Error/Success Messages -->
    <% if (error) { %>
      <div class="alert alert-danger py-2 alert-dismissible" style="font-size: 0.85rem;">
        <% if (error === 'invalid_username') { %>Username must be 3-30 characters, lowercase letters/numbers/underscore only.
        <% } else if (error === 'password_short') { %>Password must be at least 8 characters.
        <% } else if (error === 'username_taken') { %>That username is already taken.
        <% } else if (error === 'mod_not_found') { %>Mod not found.
        <% } else if (error === 'invalid_model') { %>Invalid model selection.
        <% } else if (error === 'llm_failed') { %>LLM connection failed — check API key and try again.
        <% } else { %><%= error %><% } %>
        <button type="button" class="btn-close btn-close-sm" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (msg) { %>
      <div class="alert alert-success py-2 alert-dismissible" style="font-size: 0.85rem;">
        <% if (msg === 'mod_created') { %>Mod created successfully!
        <% } else if (msg === 'llm_saved') { %>LLM settings saved.
        <% } else if (msg === 'llm_ok') { %>LLM connection test passed!
        <% } else if (msg === 'site_saved') { %>Site settings saved.
        <% } else { %>Done.<% } %>
        <button type="button" class="btn-close btn-close-sm" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <!-- ═══ MOD MANAGEMENT ═══ -->
    <div class="mb-4">
      <h2 class="section-title">Moderator Management</h2>

      <div class="panel-card">
        <% if (mods.length === 0) { %>
          <p class="text-muted text-center py-2" style="font-size: 0.85rem;">No moderators yet. Create one below.</p>
        <% } else { %>
          <% mods.forEach(mod => { %>
            <div class="mod-row">
              <div>
                <strong style="font-size: 0.9rem;"><%= mod.username %></strong>
                <span class="<%= mod.active ? 'status-active' : 'status-disabled' %> ms-2"><%= mod.active ? 'Active' : 'Disabled' %></span>
                <div style="font-size: 0.72rem; color: #999;">Created <%= new Date(mod.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></div>
              </div>
              <div class="d-flex gap-1">
                <form method="POST" action="/super/mods/<%= mod.id %>/toggle" style="display:inline;">
                  <button type="submit" class="btn btn-xs <%= mod.active ? 'btn-outline-warning' : 'btn-outline-success' %>">
                    <%= mod.active ? 'Disable' : 'Enable' %>
                  </button>
                </form>
                <form method="POST" action="/super/mods/<%= mod.id %>/delete" style="display:inline;"
                      onsubmit="return confirm('Delete mod &quot;<%= mod.username %>&quot; permanently?')">
                  <button type="submit" class="btn btn-outline-danger btn-xs">Delete</button>
                </form>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>

      <!-- Create Mod Form -->
      <div class="panel-card">
        <h6 class="fw-bold mb-2" style="font-size: 0.85rem;">Create New Mod</h6>
        <form method="POST" action="/super/mods/create" class="d-flex gap-2 align-items-end flex-wrap">
          <div>
            <label class="form-label" style="font-size: 0.75rem;">Username</label>
            <input type="text" name="username" class="form-control form-control-sm" placeholder="lowercase, 3-30 chars" required pattern="[a-z0-9_]{3,30}">
          </div>
          <div>
            <label class="form-label" style="font-size: 0.75rem;">Password</label>
            <input type="password" name="password" class="form-control form-control-sm" placeholder="8+ characters" required minlength="8">
          </div>
          <button type="submit" class="btn btn-sm btn-primary">Create Mod</button>
        </form>
      </div>
    </div>

    <!-- ═══ LLM CONFIGURATION ═══ -->
    <div class="mb-4">
      <h2 class="section-title">LLM Configuration</h2>

      <% if (!hasApiKey) { %>
        <div class="alert alert-warning py-2" style="font-size: 0.8rem;">
          <strong>No API key set.</strong> Add <code>OPENROUTER_API_KEY</code> to your environment variables to enable AI features.
        </div>
      <% } %>

      <div class="panel-card">
        <form method="POST" action="/super/settings/llm">
          <div class="row mb-3">
            <div class="col-md-6 mb-2">
              <label class="form-label fw-semibold" style="font-size: 0.8rem;">Analysis Model</label>
              <select name="analysisModel" class="form-select form-select-sm">
                <% models.forEach(m => { %>
                  <option value="<%= m.id %>" <%= settings && settings.analysisModel === m.id ? 'selected' : '' %>>
                    <%= m.name %> <% if (m.note) { %>(<%- m.note %>)<% } %>
                  </option>
                <% }) %>
              </select>
              <div class="model-note mt-1">Used for auto-analyzing submitted tips</div>
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label fw-semibold" style="font-size: 0.8rem;">Rewrite Model</label>
              <select name="rewriteModel" class="form-select form-select-sm">
                <% models.forEach(m => { %>
                  <option value="<%= m.id %>" <%= settings && settings.rewriteModel === m.id ? 'selected' : '' %>>
                    <%= m.name %> <% if (m.note) { %>(<%- m.note %>)<% } %>
                  </option>
                <% }) %>
              </select>
              <div class="model-note mt-1">Used for on-demand tip rewrites</div>
            </div>
          </div>
          <div class="d-flex gap-2">
            <button type="submit" class="btn btn-sm btn-primary">Save LLM Settings</button>
          </div>
        </form>

        <hr class="my-3">

        <form method="POST" action="/super/settings/llm-test" class="d-flex align-items-center gap-2">
          <button type="submit" class="btn btn-sm btn-outline-secondary" <%= !hasApiKey ? 'disabled' : '' %>>Test LLM Connection</button>
          <small class="text-muted">Sends a simple test prompt to the analysis model</small>
        </form>
      </div>
    </div>

    <!-- ═══ SITE SETTINGS ═══ -->
    <div class="mb-4">
      <h2 class="section-title">Site Settings</h2>

      <div class="panel-card">
        <form method="POST" action="/super/settings/site">
          <div class="mb-3">
            <label class="form-label fw-semibold" style="font-size: 0.8rem;">Board Name</label>
            <input type="text" name="boardName" class="form-control form-control-sm" value="<%= settings ? settings.boardName : 'Valle Vista Commons' %>" maxlength="100">
          </div>
          <div class="mb-3">
            <label class="form-label fw-semibold" style="font-size: 0.8rem;">Tagline</label>
            <input type="text" name="boardTagline" class="form-control form-control-sm" value="<%= settings ? settings.boardTagline : 'Your neighborhood board' %>" maxlength="200">
          </div>
          <div class="mb-3">
            <label class="form-label fw-semibold" style="font-size: 0.8rem;">About Text</label>
            <textarea name="aboutText" class="form-control form-control-sm" rows="4" maxlength="2000" placeholder="Shown at the bottom of the public board"><%= settings ? settings.aboutText || '' : '' %></textarea>
          </div>
          <button type="submit" class="btn btn-sm btn-primary">Save Site Settings</button>
        </form>
      </div>
    </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
