<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow">
  <title>Valle Vista Commons</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; }
    .brand { font-weight: 700; color: #2d6a4f; }
  </style>
</head>
<body>
  <div class="container py-4" style="max-width: 640px;">
    <h1 class="brand text-center mb-4">Valle Vista Commons</h1>

    <% if (posts.length === 0) { %>
      <div class="text-center text-muted py-5">
        <p>No posts yet. Check back soon!</p>
      </div>
    <% } else { %>
      <% posts.forEach(post => { %>
        <div class="card mb-3">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-start">
              <h5 class="card-title mb-1"><%= post.title %></h5>
              <span class="badge bg-secondary"><%= post.tag %></span>
            </div>
            <p class="card-text mt-2"><%= post.desc %></p>
            <small class="text-muted">
              📍 <%= post.location %> · <%= new Date(post.approvedAt || post.createdAt).toLocaleDateString() %>
            </small>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
