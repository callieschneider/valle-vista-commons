# Database (Railway provides this automatically when you add Postgres)
DATABASE_URL="postgresql://user:pass@host:5432/dbname"

# Super admin credentials for /super panel (REQUIRED)
SUPER_ADMIN_USER="callie"
SUPER_ADMIN_PASS="change-me-to-something-strong"

# OpenRouter API key for AI features (optional — AI disabled if empty)
# Get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=""

# hCaptcha keys (https://dashboard.hcaptcha.com) — optional
HCAPTCHA_SITEKEY=""
HCAPTCHA_SECRET=""

# Upload directory — Railway volume mounted at /uploads/ in prod
# Defaults to ./uploads locally if empty
UPLOAD_DIR=""

# Anonymous submitter tracking — random secret salt (optional)
# When set, a one-way hash of the submitter's IP is stored to assign
# sequential user numbers visible only to admins. No raw IPs are stored.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTHOR_HASH_SALT=""
